# Python
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
.venv/
venv/

# Build artifacts (PyPI dist — built locally, never committed)
build/
dist/

# Env / secrets — deny by default
.env
.env.*
# ...except the template, which carries a placeholder and no key. The
# placeholder-sync test derives its expected values from this file.
!.env.example
*.key
*.pem

# Claude Code local settings
.claude/

# OS
.DS_Store
